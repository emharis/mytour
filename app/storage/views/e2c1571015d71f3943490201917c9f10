<div class="menu classic" >
    <ul id="nav" class="menu" >
        <?php foreach($topmenu as $menu): ?>
        <li>
            <a href="<?php echo ($menu->type->id==1?'#':($menu->type->id==3?$menu->type->link.$menu->statpage_id:$menu->type->link)); ?>" ><?php echo strtoupper($menu->nama); ?></a>
            <?php if(count($menu->childmenus ) >0): ?>
            <ul class="sub-menu">
                <?php foreach($menu->childmenus as $chd): ?>
                    <li><a href="<?php echo ($chd->type->id==1?'#':($chd->type->id==3?$chd->type->link.$chd->statpage_id:$chd->type->link)); ?>"><?php echo strtoupper($chd->nama); ?></a></li>
                <?php endforeach; ?>
            </ul>
            <?php endif; ?>
        </li>
        <?php endforeach; ?>
<!--        <li class="<?php echo (Request::is('front/user/login') ? 'active':''); ?>" >
            <a id="btn-login" href="front/user/login" >LOGIN</a>
        </li>-->

    </ul>
</div>
